func main()
  var p : pair int, bool endpair
  var q : pair int, bool endpair
  var x : int
  var b : bool

  x = 10;
  while (p.first != not b) do
     x = 20*p.first + 2;
     b = p.second and not (x<0);
     if (p.second+3==0
         and not p.first) then
       write "ok";
       q = p;
     endif
  endwhile
endfunc 
